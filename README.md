populate a List of Transactions with data when the Repository is created on startup.
transactions in sorted ascending order (by date).
Also note that the IDs for each transaction is clickable. When one of them is clicked it will navigate to the URL.
Have a Controller, a Service and a Repository with properly named methods
